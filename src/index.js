import React from 'react';
import ReactDOM from "react-dom";
import { FilterableBookmarkTable } from './components/FilterableBookmarkTable'

ReactDOM.render(<FilterableBookmarkTable/>, document.getElementById("root"))
